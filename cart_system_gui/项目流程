1.需求分析（当时购物车给的题上）
2.界面设计（商品展示、购物车界面、结算、登录（分用户：管理员、普通用户）、注册）
3.ui文件转py文件  pyuic5 -x login.ui -o login.py
4.逻辑代码 （有界面了，得有每个界面的逻辑，比如获取输入、判断有无，跳转页面等）



虚拟环境(隔离)
conda create --name spt2507 python=3.12


requirements.txt 放环境变量
